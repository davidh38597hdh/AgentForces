# AgentForces positioning — go all in

**Product:** AgentForces  
**Live:** https://www.agentxforces.com  
**Thesis:** A **force** is a mesh of agents that work together toward shared outcomes — with **business boundaries**, not just another agent loop in code.

---

## Category

| We are | We are not |
|--------|------------|
| Multi-agent **mesh product** for operators & teams | A Python agent **framework** |
| Visual force design + run + identity | An embeddable orchestration **library** |
| Path to **AMEP mesh security** | LangChain/LangGraph/CrewAI clone |

---

## Vs frameworks (use this language)

| | LangGraph | CrewAI | **AgentForces** |
|--|-----------|--------|-----------------|
| **What you buy** | Runtime for agent graphs in *your* code | Crew/roles/tasks in *your* code | **Product** to design & run a **force** |
| **Interface** | Code (+ platform) | Code | **Canvas, library, inspector** |
| **Boundaries** | Your app’s job | Your app’s job | **Companies, networks, Ext interfaces** |
| **Security story** | App-level | App-level | **AMEP/1** sessions → AEAD mesh (roadmap) |
| **Identity** | Your auth | Your auth | **Google-gated mesh in production** |
| **Keys** | You wire providers | You wire providers | **BYOK per agent + token router** |

**One-liner:**  
LangGraph programs control flow. CrewAI staffs a crew. **AgentForces fields a force** — a mesh with edges that respect who can talk to whom.

---

## Pillars (product claims)

1. **Force, not framework** — ship outcomes on a mesh, don’t reimplement orchestration.  
2. **Boundaries by design** — multi-company / multi-network; Ext-only cross edges.  
3. **Chief + networks** — research · computation · creative; keyword chief routing.  
4. **Your keys** — BYOK multi-provider (xAI, OpenAI, Anthropic) per node.  
5. **Connectors** — Gmail send, Slack, webhooks as agent tools + post-run notify (BYOK).  
6. **Mesh protocol path** — AMEP/1 identity today; sealed hops as differentiator tomorrow.  
7. **Operator UX** — agent library, inspector, hop logs, session meta — not a notebook.

---

## Messaging dos / don’ts

**Do**
- “Private multi-agent mesh for business outcomes”
- “Cross-org interfaces, not a flat agent free-for-all”
- “Run a force in the browser; keep keys yours”

**Don’t**
- “LangGraph alternative” as the lead (we’re a different layer)
- Claim AEAD/sealed hops until `MESH_TRANSPORT = amep_aead`
- Lead with “open source multi-agent framework”

---

## Proof points in product (map to UI)

| Claim | Where it lives |
|-------|----------------|
| Force / mesh | Dashboard canvas, seeds, hop log |
| Boundaries | Ext edges, company colors, network ids |
| Chief routing | Orchestrate run, primary network |
| BYOK | API keys panel, token router |
| Protocol | Session epoch, AMEP/1 labels, SECURITY.md honesty |
| Operator UX | Agent library + inspector panels |

---

## North star

> The place businesses **compose, bound, and run** multi-agent forces — with identity, keys, and mesh trust — not the library they import to write one.
